# Echo Hub v1.0 Overview 💬

**Status**: Initial UI Scaffolding (Phase 6 In Progress)

Echo Hub is the unified communication command center for SynchroFlow, designed to consolidate customer interactions, provide immediate context, and streamline resolution workflows, ultimately reducing operational overhead.

---

## User Journey 🚀

The Echo Hub user journey prioritizes **speed**, **context**, and **resolution efficiency**:

1.  **Entry & Default State**:
    * User accesses via the **"Echo Inbox"** Sidenav link.
    * Loads instantly into a **three-pane layout**:
        * **Left Pane (Sources/Filters)**: Lists sources (Email, Chat, etc.) and filters (Unassigned, Mine). Defaults to a primary view (e.g., "All Unassigned").
        * **Middle Pane (Conversations)**: Lists conversations matching the filter, sorted by urgency. Shows key details (customer, subject, time, status). The top conversation is auto-selected.
        * **Right Pane (Details & Context)**: The main workspace, showing the selected conversation thread, response composer, and embedded **Context Tabs** (Customer 360, Order 360) pre-loaded for the selected conversation. 

2.  **Triage & Selection**:
    * User scans the **Conversations Pane**, guided by visual cues (unread, timers, tags).
    * User optionally selects different filters/sources in the **Left Pane**.
    * User clicks a conversation in the **Middle Pane**. The **Right Pane** updates instantly.

3.  **Contextual Understanding & Action**:
    * User reads the conversation thread.
    * User seamlessly switches between the **"Customer 360"** and **"Order 360"** tabs within the Right Pane to get full context (LTV, order history, live WMS status, financials) *without leaving the Echo Hub*.
    * User drafts a response using the composer.

4.  **Resolution & Flow**:
    * User sends the reply and optionally updates the conversation status/assignment.
    * The system **auto-advances** to the next priority conversation in the Middle Pane, updating the Right Pane accordingly to maintain workflow momentum.

---

## Current Implementation vs. Goals (v1.0 - UI Shell)

* **Planned / In Progress ✅**:
    * **Page Creation**: `/echo-hub` route and basic page component (`#FEAT(UI): Create Echo Hub page...`).
    * **3-Pane Layout**: Responsive shell using MUI/`react-resizable-panels` (`#FEAT(UI): Create Echo Hub page...`).
    * **Static Panes**: Initial components for Left, Middle, and Right panes with placeholders/static mock data (`#FEAT(UI): Implement Echo Hub Left Pane...`, `#FEAT(UI): Implement Echo Hub Middle Pane...`, `#FEAT(UI): Implement Echo Hub Right Pane Shell...`).
    * **Basic Interaction**: Linking list selection to detail view update (using static data) (`#TASK(UI): Link Echo Hub Conversation List...`).

* **To Be Implemented ⏳**:
    * API integration for fetching real conversations, sources, and filters.
    * Real-time updates (new messages, status changes).
    * Full integration of live **Customer 360** and **Order 360** data within the context tabs.
    * Response composer functionality (sending messages, using templates).
    * Status/assignment functionality.
    * "Auto-advance" workflow logic.
    * Search and advanced filtering capabilities.

---

## Next Steps

The immediate focus is building the static three-pane layout and basic components outlined in the initial Phase 6 issues. Subsequent steps will involve integrating state management for selections, followed by backend API integration for live data.