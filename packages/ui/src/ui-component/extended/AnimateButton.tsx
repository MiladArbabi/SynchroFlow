// packages/ui/src/ui-component/extended/AnimateButton.tsx
import React from 'react';

interface AnimateButtonProps {
  children: React.ReactNode;
}

// Simple passthrough stub for now
const AnimateButton: React.FC<AnimateButtonProps> = ({ children }) => {
  return <>{children}</>;
};

export default AnimateButton;