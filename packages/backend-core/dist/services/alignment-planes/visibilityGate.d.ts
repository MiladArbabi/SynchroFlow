/**
 * Visibility Gate
 * ---------------
 * Alignment planes may only execute when
 * ALL required domains are epistemically usable.
 *
 * Any deviation → unknown.
 */
export declare function visibilityGate(visibilities: Array<'sufficient' | 'insufficient' | null>): boolean;
