/**
 * assertKnown
 * -----------
 * Narrows an EpistemicValue<T> to KNOWN at compile time.
 *
 * - NO runtime behavior
 * - NO mutation
 * - Fails compilation if misused
 */
export function assertKnown(value) {
    // Intentionally empty
}
/**
 * assertIncomplete
 * ----------------
 * Narrows to INCOMPLETE epistemic state.
 */
export function assertIncomplete(value) {
    // Intentionally empty
}
//# sourceMappingURL=assertions.js.map