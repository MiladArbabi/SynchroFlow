import React from "react";

export default function ModuleLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ padding: 24 }}>
      <h2 style={{ marginBottom: 16 }}>
        {{moduleName | capitalize}} Module
      </h2>
      {children}
    </div>
  );
}
