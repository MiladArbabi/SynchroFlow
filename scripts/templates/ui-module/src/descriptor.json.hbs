{
  "id": "{{moduleId}}",
  "name": "{{pascalName}}",
  "version": "0.1.0",
  "routes": [
    {
      "id": "{{moduleId}}-home",
      "key": "{{moduleId}}-home",
      "name": "{{pascalName}} Home",
      "path": "/{{moduleId}}",
      "requiredModuleId": "{{moduleId}}",
      "order": 100
    }
  ],
  "navItems": [
    {
      "id": "{{moduleId}}-home",
      "title": "{{pascalName}}",
      "path": "/{{moduleId}}",
      "order": 50
    }
  ]
}
